# Ключи знаний

Образовательный 2D-платформер для детей 10–13 лет. Бежишь, прыгаешь, находишь сундук,
решаешь задачу по алгебре / геометрии / физике, получаешь ключ, открываешь дверь.

Ванильный JavaScript, Canvas 2D, нативные ES-модули. Без зависимостей, без сборщика,
без бэкенда — открывается как статика по одной ссылке.

## Запуск

Просто дважды кликнуть по `index.html` **нельзя**: браузер запрещает ES-модули с
`file://`. Нужен любой локальный сервер.

```bash
python -m http.server 8000
```

На macOS и Linux — `python3 -m http.server 8000`.

Дальше открыть <http://localhost:8000>.

## Управление

| Действие | Клавиши |
|---|---|
| Бежать | `←` `→` или `A` `D` |
| Прыжок | `Space`, `↑` или `W` |

WASD работает и на русской раскладке: ввод читает физический код клавиши, а не букву.

Экранные кнопки для телефона появятся на Этапе 7.

## Что сделано (Этап 1 из 7)

Каркас: игровой цикл с фиксированным шагом, ввод с клавиатуры, отрисовка тайловой
карты и игрока, базовые коллизии.

```
index.html
styles/main.css
src/
  main.js                бутстрап: собирает цикл, ввод и рендер
  engine/
    constants.js         все числа геймплея в одном месте
    loop.js              фиксированный шаг 60 Гц + alpha для интерполяции
    input.js             клавиши → игровые действия
    renderer.js          небо, тайлы, игрок — фигурами Canvas
  game/
    player.js            движение и разбор столкновений
    testMap.js           временная хардкод-карта Этапа 1
```

## Что дальше

| Этап | Что появится |
|---|---|
| 2 | `engine/physics.js`, `engine/camera.js`: coyote time, jump buffer, variable jump, чекпоинты |
| 3 | `game/entities.js`, `game/level.js`, `levels/*.json`: монеты, сундуки, ключи, двери, шипы |
| 4 | `tasks/`: движок задач, сидируемый rng, 15 генераторов, модалка с чертежами |
| 5 | `save.js`, `ui/menu.js`: прогресс, выбор уровня, звёзды, настройки |
| 6 | Пять уровней |
| 7 | Звук, экранные кнопки, доступность, деплой |

Полное ТЗ — в [SPEC.md](SPEC.md), правила работы над кодом — в [CLAUDE.md](CLAUDE.md).

## Как поправить ощущение от прыжка

Все числа лежат в [src/engine/constants.js](src/engine/constants.js) с комментариями.
Прыжок выше — увеличить `JUMP_SPEED`. Персонаж тяжелее — увеличить `GRAVITY`.
Разгон резче — увеличить `RUN_ACCELERATION`. Значения подобраны на ощупь, их можно крутить.
